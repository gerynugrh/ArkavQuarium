#include "Game.hpp"
#include <iostream>
#include <math.h>
#include <sstream>
#include <vector>

const double speed = 50; // pixels per second

void handleEvent(SDL_Event * e);

int main( int argc, char* args[] )
{
    Game game;
}